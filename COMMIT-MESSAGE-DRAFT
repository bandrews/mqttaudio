Phase 0: Initialize Rust project with dependencies and module structure

Set up the foundational project structure for mqttaudio, a Rust rewrite of the legacy C++ MQTT-controlled audio player. This phase establishes the build system, dependencies, and module organization.

Changes:
- Initialize Cargo project with edition 2021
- Add all core dependencies: cpal (audio I/O), symphonia (decoding), rubato (resampling), rumqttc (MQTT), reqwest (HTTP), tokio (async runtime)
- Create module structure:
  - src/audio/ - Audio subsystem (engine, mixer, decoder, resampler, types)
  - src/mqtt/ - MQTT client and command parsing
  - src/cache/ - Disk and memory caching
  - src/config.rs - Configuration management
  - src/voice.rs - Voice grouping
- Implement basic CLI with clap:
  - --list-devices to enumerate audio devices
  - Configuration options for server, topic, device, sample rate
  - Ctrl+C signal handling
- Implement audio device enumeration with cpal
- Add comprehensive documentation in /docs/ folder (12 files, ~120KB)
- Add ABOUTME headers to all source files

Testing:
- Project compiles successfully with `cargo check`
- CLI help displays correctly
- Audio device enumeration works on macOS (CoreAudio)
- Default device configuration retrieval works

Next phase: Implement basic audio output (440Hz sine wave test)

Prompts:
- "Please document all of these plans in great detail in the /docs folder"
- "This looks fantastic. Let's move on to implementation."

Generated with Claude Code (claude.com/claude-code)
Co-Authored-By: Claude <noreply@anthropic.com>
