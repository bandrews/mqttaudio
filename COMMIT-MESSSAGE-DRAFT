Correct documentation to match actual behavior; add review summary

Documentation pass from the quality review, aligning every checked doc
claim with what the code does:

- Remove the nonexistent --buffer-size CLI flag, the nonexistent
  environment variables (MQTTAUDIO_CONFIG, MQTTAUDIO_CACHE_DIR,
  RUST_LOG), and the wrong issue-tracker link.
- Correct defaults: http.port (0 = auto-select, not 8080), input
  latency_ms (20, not 25), stop fade_out_ms (10, not 0), volume ranges
  (0.0-4.0 gains).
- Rewrite the caching docs to actual behavior: no server revalidation
  (cache_invalidate refreshes), streamed URL plays are not persisted to
  disk, eviction does not protect playing files; mark cache.enabled and
  revalidate_after_seconds as reserved.
- Rework the microphone-ducking examples in three docs: a mic's voice
  can be ducked but cannot trigger ducking, so the old examples silently
  did nothing.
- Update security docs to the enforcement just implemented (non-empty
  allowed_directories enforced with canonicalization; empty list leaves
  local access unrestricted).
- Document previously unsurfaced features: /input/volume and
  /input/mute endpoints, the internal_id selector, the
  soundFadeAll/fadeout/soundFadeOut aliases, --http-port and
  --max-cache-mb, mqtt.client_id and reconnect_delay_seconds, and the
  fire-and-forget nature of HTTP command responses.
- Describe /ws honestly (connects but streams no logs) and drop the
  WebSocket claims from the README until the feature is wired up.
- Fix broken install steps (stale -b refactor clone, placeholder repo
  URL, Windows prerequisites contradiction) and example-config notes.
- Add docs/quality-review-2026-08/summary.md and point docs/bugs.md at
  the review folder as the current backlog of known issues.

Prompts that led to this work:
- Please do a thorough quality review of mqttaudio looking for buggy
  features, settings that are unimplemented, usability gaps, unsurfaced
  settings and other issues that may trip up users of the product.
  Record your finding in a folder under /docs/. Then, triage the
  findings, and fix issues that are obviously broken and needing to be
  addressed. Produce a summary of all issues, both the ones you fix and
  the ones that are deferred for a decision.

This commit was authored by Claude (AI assistant) working with Ben Andrews.
