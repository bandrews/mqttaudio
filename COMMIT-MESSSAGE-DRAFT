Quality review: record findings for cache/HTTP subsystem

Start of a full product quality review. This commit records the first
batch of verified findings (cache and HTTP subsystems) under
docs/quality-review-2026-08/. Highlights: security.allowed_directories
is parsed and documented but never enforced; a stalled HTTP download can
wedge the command loop and silence audio via lock chaining; the
WebSocket log stream is never wired into tracing; disk-cache
revalidation and cache.enabled are unimplemented. Further findings
(audio engine, commands/config, docs consistency), triage, and fixes
follow in subsequent commits on this branch.

Prompts that led to this work:
- Please do a thorough quality review of mqttaudio looking for buggy
  features, settings that are unimplemented, usability gaps, unsurfaced
  settings and other issues that may trip up users of the product.
  Record your finding in a folder under /docs/. Then, triage the
  findings, and fix issues that are obviously broken and needing to be
  addressed. Produce a summary of all issues, both the ones you fix and
  the ones that are deferred for a decision.

This commit was authored by Claude (AI assistant) working with Ben Andrews.
